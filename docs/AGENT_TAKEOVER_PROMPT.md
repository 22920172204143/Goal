# Goal 项目接管提示词

下面这段可以直接发给下一位 agent 使用。

---

你现在接管的是 `Goal`（Flutter）项目，根目录在 `E:\AiProgram\Goal`。  
目标是继续完成“收入 -> 房间反馈”的产品体验打磨，不要重做基础架构。

## 你接手前请先确认

1. 项目可运行：
   - `flutter pub get`
   - `flutter run -d emulator-5556`
2. Android 模拟器固定使用 `goal`（常见设备 ID 为 `emulator-5556`）
3. 包名是 `com.yourcompany.smallgoal.small_goal_app`

## 当前实现背景

- 数据层：收入记录、统计、目标、连续记录已经稳定
- 首页房间舞台：`lib/widgets/star_office_room_stage.dart`
- 素材目录：`assets/star_office/`
- 参考风格：`Star-Office-UI`（已做素材复用）
- 关键修复：安卓端错帧/乱码条纹已通过“单帧切换”方式规避

## 接下来优先级（按顺序）

1. **财富反馈精修**
   - 按 `desk/cabinet/floor` 三档做更清晰层级反馈
   - 保持“看一眼就知道最近赚了多少”的可读性
2. **角色互动增强**
   - 优化不同状态的落点、动作节奏和区域氛围
   - 避免角色遮挡关键家具
3. **视觉统一**
   - 清理临时道具，统一材质风格、阴影和比例
4. **交付可维护**
   - 代码改动集中在现有组件，不破坏控制器与服务层接口

## 约束

- 不要提交构建缓存、IDE 临时文件、本地截图调试图
- 不要随意改包名、统计口径和已有持久化字段
- 每轮改动后至少跑一次 `flutter analyze`
- 若涉及大改，先给出 3-5 条最小改动计划再实施

## 输出要求

- 每次提交附带：
  - 变更点
  - 影响范围
  - 验证方式（至少一条设备验证）
- 若遇阻塞（插件、模拟器、签名冲突），优先给出可执行的最短修复路径

---

接管后请先从 `lib/widgets/star_office_room_stage.dart` 和 `README.md` 开始阅读，再继续迭代。
